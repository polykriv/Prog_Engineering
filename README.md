# Тема_4: Функции и модули

- Студентка: Кривощекова Полина Андреевна
- Группа: ИВТ-23-1

# Выполненные задания
| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + |   |
| Задание 7 | + |   |
| Задание 8 | + |   |
| Задание 9 | + |   |
| Задание 10 | + |   |

#Лабораторная работа по Python
## №1
Напишите функцию, которая выполняет любые арифметические действия и выводит результат в консоль. Вызовите функцию используя
"точку входа".

```  python
set_1 = {'White', 'Black', 'Red', 'Pink'}
set_2 = {'Red', 'Green', 'Blue', 'Red'}

print(set_1 - set_2)
```

### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/1.png)

### Вывод:
В задании показано создание и вызов функции, которая выполняет арифметическую операцию (сложение) и выводит результат. Использование конструкции "точка входа" позволяет корректно запустить функцию при выполнении скрипта.

## №2
Напишите функцию, которая выполняет любые арифметические действия, возвращает при помощи return значение в место, откуда вызывали функцию. Выведите результат в консоль. Вызовите функцию используя "точку входа".

```
a = set('abcdefg')
print(a)
for i in range(1, 5):
    a.add(i)
print(a)
```

### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/2.png)

### Вывод:
Демонстрируется функция с возвратом результата через return. Значение, возвращенное функцией, присваивается переменной и выводится, что иллюстрирует передачу данных из функции.

## №3
Напишите функцию, в которую передаются два аргумента, над ними производится арифметическое действие, результат возвращается туда, откуда эту функцию вызывали. Выведите результат в консоль.
Вызовите функцию в любом небольшом цикле.
На скриншоте ниже приведен пример программы, в которой аргумент функции в
""превращается в параметр "опе", то же самое происходит
c"y"и "two"

```
def replace(input_list):
    memory = input_list[0]
    input_list[0] = input_list[-1]
    input_list[-1] = memory

    return input_list

print(replace([1, 2, 3, 4, 5]))
```

### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/3.png)

### Вывод:
Задание закрепляет передачу аргументов в функцию и выполнение над ними операции. Цикл позволяет выполнить функцию несколько раз с одинаковыми параметрами, демонстрируя повторное вычисление результата.

## №4
Напишите функцию, на вход которой подается какое-то изначальное неизвестное количество аргументов, над которыми будет производится арифметические действия. Для выполнения задания необходимо использовать кортеж "*args". На скриншоте ниже приведен пример такой программы с комментариями.
Для закрепления понимания работы с кортежами настоятельно рекомендуем поменять аргументы вызова функции, вручную посчитать результат, только потом запустить программу с новыми значениями и проверить себя, насколько вы поняли данный аспект программирования.

```
a = [12, 54, 345, 4543, 6565, 6775, 87878, 87865, 898767, 978675]
print(a[2:6])
```

### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/4.png)

### Вывод:
Программа иллюстрирует работу с переменным количеством аргументов (*args) через кортеж, суммирование элементов и вычисление среднего значения. Это типичный пример работы с варьируемым вводом.

## №5
Напишите функцию, которая на вход получает кортеж "**kwargs" и при помощи цикла выводит значения, поступившие в функцию. На скриншоте ниже указаны два варианта вызова функции с "**kwargs" и два варианта работы с данными, поступившими в эту функцию.
Комментарии в коде и теоретическая часть помогут вам разобраться в этом нелегком аспекте. Вызовите функцию используя "точку входа".

```
def useless(lst):
    return max(lst) / len(lst)

print(useless([3, 4, 65, 34, 6]))
print(useless([-34, 545, 34.4, 45.3, 654.34, 65.654, 345, 65, 23, 56.43]))
print(useless([334, -42, 45, 34.2, -543, -342.342, 54, 34, -23]))
```

### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/5.png)

### Вывод:
Рассмотрена работа с именованными параметрами в виде словаря (**kwargs). Показано, как можно пройти через элементы словаря и вывести ключи с соответствующими значениями с использованием цикла.

## №6
Напишите две функции. Первая - получает в виде параметра
"**kwargs". Вторая считает среднее арифметическое из значений первой функции. Вызовите первую функцию используя "точку входа" и минимум 4 аргумента.

```
superhoroes = ['superman', 'spiderman', 'batman']

nikolay, vasiliy, ivan = superhoroes

print('Николай -', nikolay)
print('Василий -', vasiliy)
print('Иван -', ivan)
```

### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/6.png)

### Вывод:
Задание расширяет использование **kwargs, демонстрируя вызов вспомогательной функции для вычисления среднего арифметического каждого списка значений. Это пример модульного подхода в программировании.

## №7
Создайте дополнительный файл .ру. Напишите в нем любую функцию, которая будет что угодно выводить в консоль, но не вызывайте ее в нем. Откройте файл main.py, импортируйте в него функцию из нового файла и при помощи "точки входа" вызовите эту функцию.

```
a = [-34.2, 32, 34.34, 45, -54, 45.3, 45, -543]
a.sort()
print('Отсортированный список:\n', a)
a.pop(0)
print('Отсортированный список без наименьшего элемента:\n', a)
```

### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/7.png)

### Вывод:
Продемонстрирован импорт пользовательской функции из отдельного модуля и ее вызов через точку входа, что соответствует практике организации многомодульных проектов.

## №8
Напишите программу, которая будет выводить корень, синус, косинус полученного от пользователя числа.

```
from random import randint

def list_maker():
    a = [randint(1, 100)] * randint(3, 10)
    return a

if __name__ == '__main__':
    result = []
    for i in range(randint(1, 5)):
        result.append(list_maker())

    print(result)
```

### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/8.png)
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/8.2.png)
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/8.3.png)

### Вывод:
Использование стандартного модуля math для выполнения математических вычислений (корень, синус, косинус) на примере пользовательского ввода показывает интеграцию функций сторонних библиотек и работу с ними.

## №9
Напишите программу, которая будет рассчитывать какой день недели будет через п-нное количество дней, которые укажет пользователь.

```
def superset(set_1, set_2):
    if set_1 > set_2:
        print(f'Объект {set_1} является чистым супермножеством')
    elif set_1 < set_2:
        print(f'Объект {set_2} является чистым супермножеством')
    elif set_1 == set_2:
        print(f'Множества равны')
    else:
        print('Супермножества не обнаружены')

if __name__ == '__main__':
    superset({1, 4, 5, 3}, {23 ,4})
    superset({1, 4, 5, 3}, {4, 23, 4, 6})
    superset({23, 4}, {4, 23, 4, 6})
    superset({45, 454}, {23, 4})
```

### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/9.png)

### Вывод:
Программа вычисляет дату и день недели через заданное количество дней вперед с использованием модуля datetime. Это пример работы с датами и временными промежутками.

## №10
Напишите программу с использованием глобальных переменных, которая будет считать площадь треугольника или прямоугольника в зависимости от того, что выберет пользователь. Получение всей необходимой информации реализовать через input, а подсчет площадей выполнить при помощи функций. Результатом программы будет число, равное площади, необходимой фигуры.

```
my_list = [2, 5, 8, 3]
print(my_list[::-1])
```
### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/10.png)

### Вывод:
Реализовано использование глобальных переменных и функций для разных вариантов вычисления площади фигуры по выбору пользователя. Это демонстрирует передачу данных и изменение глобального состояния.

## №11
Выведите в консоль булевую переменную False, не используя слово False в строке или изначально присвоенную булевую переменную. Программа должна занимать не более двух строк редактора кода.

```
def analyze_restaurant_visits(visits):

    num_checks = len(visits)
    unique_visitors = len(set(visits))

    visitor_counts = {}
    for visitor in visits:
        if visitor in visitor_counts:
            visitor_counts[visitor] += 1
        else:
            visitor_counts[visitor] = 1

    most_frequent_visitor = None
    max_visits = 0
    for visitor, count in visitor_counts.items():
        if count > max_visits:
            most_frequent_visitor = visitor
            max_visits = count

    print("--- Статистика посещений ресторана ---")
    print(f"Всего выдано чеков: {num_checks}")
    print(f"Количество уникальных посетителей: {unique_visitors}")
    if most_frequent_visitor:
        print(f"Работник, посетивший ресторан больше всех раз: {most_frequent_visitor} (посещений: {max_visits})")
    else:
        print("Посещений не было.")

# Данные о посещениях ресторана (пример)
restaurant_visits = [8734, 2345, 8201, 6621, 9999, 1234, 5678, 8201, 8888, 4321, 3365,
                    1478, 9865, 5555, 7777, 9998, 1111, 2222, 3333, 4444, 5656, 6666,
                    5410, 7778, 8889, 4445, 1439, 9604, 8201, 3365, 7502, 3016, 4928,
                    5837, 8201, 2643, 5017, 9682, 8530, 3250, 7193, 9051, 4506, 1987,
                    3365, 5410, 7168, 7777, 9865, 5678, 8201, 4445, 3016, 4506, 4506]

analyze_restaurant_visits(restaurant_visits)
```

### Вывод:
Программа вычисляет расстояние до точки в 2D-пространстве, используя именованные аргументы и математические операции с импортированными функциями. Показан прием измерения времени выполнения кода.

## №12
Напишите программу, которая будет заменять игральную кость с 6 гранями. Если значение равно 5 или 6, то в консоль выводится «Вы победили», если значения 3 или 4, то вы рекурсивно должны вызвать эту же функцию, если значение 1 или 2, то в консоль выводится «Вы проиграли». При этом каждый вызов функции необходимо выводить в консоль значение "кубика". Для выполнения задания необходимо
использовать стандартную библиотеку random. Программу нужно написать, используя одну функцию и "точку входа"

```
def analyze_running_results(results):

    sorted_results = sorted(results)
    top_3 = sorted_results[:3]
    worst_3 = sorted_results[-3:]
    results_from_10 = results[9:]

    print("Анализ результатов бега")
    print(f"Три лучших результата: {top_3}")
    print(f"Три худших результата: {worst_3}")
    print(f"Результаты начиная с 10-го: {results_from_10}")


# Данные о результатах бега (пример)
running_results = [10.2, 14.8, 19.3, 22.7, 12.5, 33.1, 38.9, 21.6, 26.4, 17.1, 30.2,
                   35.7, 16.9, 27.8, 24.5, 16.3, 18.7, 31.9, 12.9, 37.4]

analyze_running_results(running_results)
```

### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/%D0%A12.png)

### Вывод:
Программа реализует рекурсивный вызов функции с использованием генератора случайных чисел для имитации броска кубика. Продемонстрирована логика повторного вызова и контроль результата.

## №13
Напишите программу, которая будет выводить текущее время, с точностью до секунд на протяжении 5 секунд. Программу нужно написать с использованием цикла. Подсказка: необходимо использовать модуль datetime и time, а также вам необходимо как-то "усыплять" программу на 1 секунду.

```
import math

def calculate_triangle_area(a, b, c):
    if a + b <= c or a + c <= b or b + c <= a:
        return None

    s = (a + b + c) / 2
    area = math.sqrt(s * (s - a) * (s - b) * (s - c))
    return area


def analyze_triangle_sides(list1, list2, list3):
    max_sides = [max(list1), max(list2), max(list3)]
    min_sides = [min(list1), min(list2), min(list3)]

    area_max = calculate_triangle_area(max_sides[0], max_sides[1], max_sides[2])
    area_min = calculate_triangle_area(min_sides[0], min_sides[1], min_sides[2])

    print("Площади треугольников")
    if area_max:
        print(f"Площадь треугольника из максимальных сторон: {area_max:.2f}")
    else:
        print("Треугольник из максимальных сторон не существует.")

    if area_min:
        print(f"Площадь треугольника из минимальных сторон: {area_min:.2f}")
    else:
        print("Треугольник из минимальных сторон не существует.")


# Данные о сторонах треугольников
one = [12, 25, 3, 48, 71]
two = [5, 18, 40, 62, 98]
three = [4, 21, 37, 56, 84]

analyze_triangle_sides(one, two, three)
```

### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/%D0%A13.png)

### Вывод:
Показано использование бессрочного цикла с ограничением времени по условию и корректное использование пауз между итерациями для вывода текущего времени. Это пример синхронизации и работы с временем.

## №14
Напишите программу, которая считает среднее арифметическое от аргументов вызываемое функции, с условием того, что изначальное количество этих аргументов неизвестно. Программу необходимо реализовать используя одну функцию и "точку входа".

```
def fix_grades(grades):

    new_grades = [4 if grade == 3 else grade for grade in grades if grade != 2]
    return new_grades

grades1 = [2, 3, 4, 5, 3, 4, 5, 2, 2, 5, 3, 4, 3, 5, 4]
grades2 = [4, 2, 3, 5, 3, 5, 4, 2, 2, 5, 4, 3, 5, 3, 4]
grades3 = [5, 4, 3, 3, 4, 3, 3, 5, 5, 3, 3, 3, 3, 4, 4]

print("Исправление оценок Борисом")
print(f"Оценки до: {grades1}, После: {fix_grades(grades1)}")
print(f"Оценки до: {grades2}, После: {fix_grades(grades2)}")
print(f"Оценки до: {grades3}, После: {fix_grades(grades3)}")
```

### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/%D0%A14.png)

### Вывод:
Демонстрация написания функции со случайным числом аргументов и возвращением усредненного значения — удобный и гибкий способ обработки нескольких значений.

## №15
Создайте два Python файла, в одном будет выполняться вычисление площади треугольника при помощи формулы Герона (необходимо реализовать через функцию), а во втором будет происходить взаимодействие с пользователем (получение всей необходимой информации и вывод результатов). Напишите эту программу и выведите в консоль полученную площадь.

```
def create_set_with_duplicates(data_list):

    counts = {}
    for num in data_list:
        if num in counts:
            counts[num] += 1
        else:
            counts[num] = 1

    result_set = set()
    for num, count in counts.items():
        result_set.add(num)

        for i in range(2, count + 1):
            result_set.add(str(num) * i)

    return result_set


# Тестовые данные
list_1 = [1, 1, 3, 3, 1]
list_2 = [5, 5, 5, 5, 5, 5, 5]
list_3 = [2, 2, 1, 2, 2, 5, 6, 7, 1, 3, 2, 2]

set_1 = create_set_with_duplicates(list_1)
set_2 = create_set_with_duplicates(list_2)
set_3 = create_set_with_duplicates(list_3)

print("Множество 1:", set_1)
print("Множество 2:", set_2)
print("Множество 3:", set_3)
```

### Результат
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/%D0%A15.1.png)
![Меню](https://github.com/polykriv/Prog_Engineering/blob/Tema_4/Screen/%D0%A15.2.png)

### Вывод:
Задание разбито на два модуля — вычислительный и интерфейсный, что соответствует принципам разделения ответственности. Введена проверка на существование треугольника, демонстрируется вычисление площади по формуле Герона.

